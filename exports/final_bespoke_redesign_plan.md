# Bespoke redesign plan

This plan was regenerated from the current source tree after the previous audit artifacts were deleted. It contains every page whose page-level component is imported by more than one route. Each replacement component name is unique and each page receives a distinct layout, mechanic, mood, and emotional beat.

## Summary

| Metric | Value |
|---|---:|
| Page files scanned | 304 |
| Unique page slugs | 304 |
| Duplicate page slugs | none |
| Pages requiring bespoke components | 28 |
| Shared component groups | 13 |

## Required redesigns

### 1. Bhuntu Love Polaroids (/bhuntu-love-polaroids)

This page currently imports **BhuntuLovePolaroids**, which is shared by 2 page(s). Replace it with **BespokeBhuntuLovePolaroidsScene**. The bespoke direction is a cinematic vertical reveal using gesture-led reveal. Use a warm lacquer and ink visual system and make the emotional beat **recognition**. The replacement must contain no imports from the shared component group.

### 2. Room3 (/room3)

This page currently imports **BhuntuLovePolaroids**, which is shared by 2 page(s). Replace it with **BespokeRoom3Scene**. The bespoke direction is a editorial split narrative using drag-and-place composition. Use a midnight blue and silver visual system and make the emotional beat **quiet gratitude**. The replacement must contain no imports from the shared component group.

### 3. Bhuntu Trivia Showdown (/bhuntu-trivia-showdown)

This page currently imports **BhuntuTriviaShowdown**, which is shared by 2 page(s). Replace it with **BespokeBhuntuTriviaShowdownScene**. The bespoke direction is a interactive map chamber using cursor-lit discovery. Use a apricot paper and charcoal visual system and make the emotional beat **playful surprise**. The replacement must contain no imports from the shared component group.

### 4. Room4 (/room4)

This page currently imports **BhuntuTriviaShowdown**, which is shared by 2 page(s). Replace it with **BespokeRoom4Scene**. The bespoke direction is a tactile paper laboratory using timed sensory transition. Use a electric violet on black visual system and make the emotional beat **earned vulnerability**. The replacement must contain no imports from the shared component group.

### 5. Bhuntu Voice Note Archive (/bhuntu-voice-note-archive)

This page currently imports **BhuntuVoiceNoteArchive**, which is shared by 2 page(s). Replace it with **BespokeBhuntuVoiceNoteArchiveScene**. The bespoke direction is a ambient audio stage using voice-reactive response. Use a moss green and brass visual system and make the emotional beat **future longing**. The replacement must contain no imports from the shared component group.

### 6. Room10 (/room10)

This page currently imports **BhuntuVoiceNoteArchive**, which is shared by 2 page(s). Replace it with **BespokeRoom10Scene**. The bespoke direction is a constellation workspace using progressive scroll transformation. Use a frosted blue and pearl visual system and make the emotional beat **celebration**. The replacement must contain no imports from the shared component group.

### 7. Bonus Arcade (/bonus-arcade)

This page currently imports **WorldShell**, which is shared by 2 page(s). Replace it with **BespokeBonusArcadeScene**. The bespoke direction is a phone-native conversation using tap-sequenced micro-story. Use a wine red and cream visual system and make the emotional beat **relief**. The replacement must contain no imports from the shared component group.

### 8. Curated Journey (/curated-journey)

This page currently imports **WorldShell**, which is shared by 2 page(s). Replace it with **BespokeCuratedJourneyScene**. The bespoke direction is a masonry memory cabinet using freeform canvas exploration. Use a sunrise coral and sand visual system and make the emotional beat **wonder**. The replacement must contain no imports from the shared component group.

### 9. Bucket List (/bucket-list)

This page currently imports **TravelBucketList**, which is shared by 2 page(s). Replace it with **BespokeBucketListScene**. The bespoke direction is a single-word typography room using long-press unlock. Use a monochrome editorial visual system and make the emotional beat **devotion**. The replacement must contain no imports from the shared component group.

### 10. Room8 (/room8)

This page currently imports **TravelBucketList**, which is shared by 2 page(s). Replace it with **BespokeRoom8Scene**. The bespoke direction is a kinetic timeline using choice-driven branching. Use a amber firelight visual system and make the emotional beat **restful closeness**. The replacement must contain no imports from the shared component group.

### 11. Distance (/distance)

This page currently imports **NepalgunjToOsakaFlightSim**, which is shared by 3 page(s). Replace it with **BespokeDistanceScene**. The bespoke direction is a cinematic vertical reveal using gesture-led reveal. Use a warm lacquer and ink visual system and make the emotional beat **recognition**. The replacement must contain no imports from the shared component group.

### 12. Nepalgunj To Osaka Flight Sim (/nepalgunj-to-osaka-flight-sim)

This page currently imports **NepalgunjToOsakaFlightSim**, which is shared by 3 page(s). Replace it with **BespokeNepalgunjToOsakaFlightSimScene**. The bespoke direction is a editorial split narrative using drag-and-place composition. Use a midnight blue and silver visual system and make the emotional beat **quiet gratitude**. The replacement must contain no imports from the shared component group.

### 13. Room2 (/room2)

This page currently imports **NepalgunjToOsakaFlightSim**, which is shared by 3 page(s). Replace it with **BespokeRoom2Scene**. The bespoke direction is a interactive map chamber using cursor-lit discovery. Use a apricot paper and charcoal visual system and make the emotional beat **playful surprise**. The replacement must contain no imports from the shared component group.

### 14. First Moments Timeline (/first-moments-timeline)

This page currently imports **FirstMomentsTimeline**, which is shared by 3 page(s). Replace it with **BespokeFirstMomentsTimelineScene**. The bespoke direction is a tactile paper laboratory using timed sensory transition. Use a electric violet on black visual system and make the emotional beat **earned vulnerability**. The replacement must contain no imports from the shared component group.

### 15. Room6 (/room6)

This page currently imports **FirstMomentsTimeline**, which is shared by 3 page(s). Replace it with **BespokeRoom6Scene**. The bespoke direction is a ambient audio stage using voice-reactive response. Use a moss green and brass visual system and make the emotional beat **future longing**. The replacement must contain no imports from the shared component group.

### 16. Story (/story)

This page currently imports **FirstMomentsTimeline**, which is shared by 3 page(s). Replace it with **BespokeStoryScene**. The bespoke direction is a constellation workspace using progressive scroll transformation. Use a frosted blue and pearl visual system and make the emotional beat **celebration**. The replacement must contain no imports from the shared component group.

### 17. Love Coronation Ceremony (/love-coronation-ceremony)

This page currently imports **LoveCoronationCeremony**, which is shared by 2 page(s). Replace it with **BespokeLoveCoronationCeremonyScene**. The bespoke direction is a phone-native conversation using tap-sequenced micro-story. Use a wine red and cream visual system and make the emotional beat **relief**. The replacement must contain no imports from the shared component group.

### 18. Room11 (/room11)

This page currently imports **LoveCoronationCeremony**, which is shared by 2 page(s). Replace it with **BespokeRoom11Scene**. The bespoke direction is a masonry memory cabinet using freeform canvas exploration. Use a sunrise coral and sand visual system and make the emotional beat **wonder**. The replacement must contain no imports from the shared component group.

### 19. Love Envelope (/love-envelope)

This page currently imports **LoveEnvelope**, which is shared by 2 page(s). Replace it with **BespokeLoveEnvelopeScene**. The bespoke direction is a single-word typography room using long-press unlock. Use a monochrome editorial visual system and make the emotional beat **devotion**. The replacement must contain no imports from the shared component group.

### 20. Room7 (/room7)

This page currently imports **LoveEnvelope**, which is shared by 2 page(s). Replace it with **BespokeRoom7Scene**. The bespoke direction is a kinetic timeline using choice-driven branching. Use a amber firelight visual system and make the emotional beat **restful closeness**. The replacement must contain no imports from the shared component group.

### 21. Love Grand Finale (/love-grand-finale)

This page currently imports **LoveGrandFinale**, which is shared by 2 page(s). Replace it with **BespokeLoveGrandFinaleScene**. The bespoke direction is a cinematic vertical reveal using gesture-led reveal. Use a warm lacquer and ink visual system and make the emotional beat **recognition**. The replacement must contain no imports from the shared component group.

### 22. Room12 (/room12)

This page currently imports **LoveGrandFinale**, which is shared by 2 page(s). Replace it with **BespokeRoom12Scene**. The bespoke direction is a editorial split narrative using drag-and-place composition. Use a midnight blue and silver visual system and make the emotional beat **quiet gratitude**. The replacement must contain no imports from the shared component group.

### 23. Love Love Story Comic Strip (/love-love-story-comic-strip)

This page currently imports **LoveLoveStoryComicStrip**, which is shared by 2 page(s). Replace it with **BespokeLoveLoveStoryComicStripScene**. The bespoke direction is a interactive map chamber using cursor-lit discovery. Use a apricot paper and charcoal visual system and make the emotional beat **playful surprise**. The replacement must contain no imports from the shared component group.

### 24. Room5 (/room5)

This page currently imports **LoveLoveStoryComicStrip**, which is shared by 2 page(s). Replace it with **BespokeRoom5Scene**. The bespoke direction is a tactile paper laboratory using timed sensory transition. Use a electric violet on black visual system and make the emotional beat **earned vulnerability**. The replacement must contain no imports from the shared component group.

### 25. Love Scratch Card (/love-scratch-card)

This page currently imports **LoveScratchCard**, which is shared by 2 page(s). Replace it with **BespokeLoveScratchCardScene**. The bespoke direction is a ambient audio stage using voice-reactive response. Use a moss green and brass visual system and make the emotional beat **future longing**. The replacement must contain no imports from the shared component group.

### 26. Room1 (/room1)

This page currently imports **LoveScratchCard**, which is shared by 2 page(s). Replace it with **BespokeRoom1Scene**. The bespoke direction is a constellation workspace using progressive scroll transformation. Use a frosted blue and pearl visual system and make the emotional beat **celebration**. The replacement must contain no imports from the shared component group.

### 27. Promise Tree (/promise-tree)

This page currently imports **SakuraPromiseTree**, which is shared by 2 page(s). Replace it with **BespokePromiseTreeScene**. The bespoke direction is a phone-native conversation using tap-sequenced micro-story. Use a wine red and cream visual system and make the emotional beat **relief**. The replacement must contain no imports from the shared component group.

### 28. Room9 (/room9)

This page currently imports **SakuraPromiseTree**, which is shared by 2 page(s). Replace it with **BespokeRoom9Scene**. The bespoke direction is a masonry memory cabinet using freeform canvas exploration. Use a sunrise coral and sand visual system and make the emotional beat **wonder**. The replacement must contain no imports from the shared component group.
